<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="a00959419.comp3717.bcit.ca.android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".screenMain">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".screenDiscoveries"></activity>
        <activity android:name=".screenDiscovery"></activity>
        <activity android:name=".screenPaused"></activity>
        <activity android:name=".screenSettings"></activity>
        <activity android:name=".screenTutorial"></activity>
    </application>

</manifest>